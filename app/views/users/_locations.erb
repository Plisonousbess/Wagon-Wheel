<% p '@' * 40 %>
<% p @locations %>
<% @locations.each_with_index do |location, i| %>
	<a id="<%= i %>" href="/locations/<%=location%>"><h4><%=location%><h4></a>
    <!-- we need the users for a particular location -->
    <!-- User.where(location: location).sort -->
<%end%>

